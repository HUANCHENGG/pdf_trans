# PDF 翻译工具（电子行业版）项目规划

> 版本：v0.3（实施状态同步） | 更新于 2026-08-29
> 修订记录：v0.1 初稿 → v0.2 对抗审查修复 → **v0.3 实施状态同步（M0-M3 已基本完成，实际形态与计划的差异以本文档标注为准；运行架构详见 docs/architecture.md）**
> 定位：面向单片机/电子行业数据手册、协议手册的 AI 翻译 Web 工具，追求 PDF 原版式保持（表格、图片、公式）。

---

## 1. 项目概述

**一句话**：自用的本地/服务器可部署 Web 工具，上传电子行业 PDF（数据手册、协议手册），接入自定义大模型 API（支持多端点多模型），输出保持原版式的**双语对照**翻译 PDF。

**核心理念**：不做通用 PDF 翻译器，聚焦电子行业文档的三大痛点——术语准确性、表格完整性、不可翻译内容保护（型号/引脚/单位/寄存器）。

### 1.1 格式保持分级验收标准（v0.2 新增，回应审查 P0-2）

"格式不变"按以下三级验收，避免"格式不变"这个模糊承诺引发的验收争议：

| 级别 | 定义 | 验收态度 |
|------|------|----------|
| **L1 结构不变** | 图片/电路图/表格/页面框架位置不变，不丢失、不裁切、不漂移 | **红线**，任何一条不达标即不可发布 |
| **L2 排版微变** | 因双语插入译文导致的行距、段落间距调整 | 可接受 |
| **L3 页面重排** | 密集页内容被挤到下一页、目录页码失效 | **已知限制**，明确接受（双语模式固有代价）；结果页需展示受影响页清单 |

### 1.2 v1 验收红线（L1 级）

1. ❌ 表格结构错乱（行列错位、边框丢失、单元格串行）
2. ❌ 图片/电路图丢失、被裁切或位置漂移
3. ❌ 译文溢出遮挡（超格溢出、行间重叠、中文乱码方块）
4. ❌ 数字/型号/引脚名被误译（`3.3V`→`3.3伏`、GPIO 被翻译、`SOP-8` 被改动）
5. ❌ 专用名词翻译错误/不一致（capacitor 同一文档两种译法）
6. ❌（v0.2 新增）**静默失败**：无文字层页/乱码编码页被无声跳过而不告知用户

---

## 2. 已确认的需求清单（两轮问答 + 对抗审查决策）

| # | 决策点 | 结论 | 架构影响 |
|---|--------|------|----------|
| 1 | 使用者 | 自己/工程师自用 | 无账号系统、无多租户；单进程 Web 应用 + 简单任务列表 |
| 2 | PDF 类型 | 原生电子版为主（文字可选中） | v1 不做 OCR，走"文本层替换"路线 |
| 3 | 输出形态 | 双语对照版（L3 重排已接受为已知限制） | 原位上下双行显示，可回查原文 |
| 4 | 图片内文字 | v1 不翻译，保留原图 | 复杂度降低一个量级 |
| 5 | 术语机制 | 预置词表 + 可编辑；硬映射为"prompt 强约束 + 译后校验告警"（见 3.4-B） | 术语一致性 must-have |
| 6 | AI API | 已有 OpenAI 兼容 API；**多端点多模型配置，任务级自选档位**（v0.2 确认） | 配置页管理多套端点，含价格信息 |
| 7 | 服务器暴露面 | **公网部署 + 共享 token 鉴权**（v0.2 决策，回应 P0-1） | M1 加环境变量 token 中间件，零账号体系下防滥用 |
| 8 | 文档规模 | **500 页+ / 200MB 上限**（v0.2 决策，回应 P2-5） | 按页分批处理 + 断点续翻为 v1 必备而非优化项 |
| 9 | 部署 | 本地 Windows 直跑；Linux 服务器 Docker | Docker Compose + 部署文档 |

---

## 3. 技术方案

### 3.1 总体架构（二开策略：以 BabelDOC 为引擎，API 优先、CLI 兜底）

```
┌─────────────────────────────────────────────────────┐
│ Web 前端                                             │
│  上传(可试翻前N页) → 端点/模型选择 → 实时进度 →          │
│  结果页(PDF预览 + 覆盖度报告 + 跳过内容审计 + 花费统计)    │
│  术语表管理页(预置词表 + CRUD + 导入导出)                │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP (SSE 进度推送)  [共享 token 鉴权]
┌──────────────────────▼──────────────────────────────┐
│ FastAPI 后端（单进程；CPU 渲染走线程/进程池）             │
│  ├─ 鉴权：共享 token 中间件（环境变量，防公网滥用）        │
│  ├─ 任务管理：串行队列 + 任务状态落库（SQLite）           │
│  ├─ 大文档处理：按页分批 + 断点续翻 + 磁盘/内存上限        │
│  ├─ 翻译编排层 ★核心开发区★                            │
│  │   ├─ 引擎适配：BabelDOC Python API 优先，            │
│  │   │           CLI 子进程调用兜底（M0 验证后定）        │
│  │   ├─ 批处理对齐协议：编号ID + 返回校验 + 失败降级逐条 ★   │
│  │   ├─ 不翻译规则引擎：片段级正则 + 表格列级规则           │
│  │   ├─ 术语表：prompt 强约束注入 + 译后校验告警           │
│  │   └─ 翻译缓存：内容哈希 × 上下文指纹                  │
│  ├─ 质量护栏：覆盖度报告 / 长度校验 / 图片对账 / 漏译对账    │
│  ├─ 术语表存储（SQLite，JSON 导入导出）                  │
│  └─ AI 适配层：多端点配置(base_url/model/key/价格)，      │
│                usage 缺失时按字符数估费兜底               │
└──────────────────────┬──────────────────────────────┘
              ┌────────▼────────┐
              │ 大模型 API（多套）│
              └─────────────────┘
```

### 3.2 为什么选 BabelDOC 作为底座

- 开源项目 **PDFMathTranslate (pdf2zh)** 及其新一代引擎 **BabelDOC** 已完整实现"AI 翻译 + PDF 原位替换 + 双语对照"，与本需求高度重合
- BabelDOC 提供 Python API，已内建：公式保护、中文字体嵌入、双语版式、OpenAI 兼容后端、glossary 接口
- 自研同样功能预计 3 个月起，二开聚焦差异层预计 6~7 周可用

**二开原则**：不 fork 修改引擎源码；通过官方 Python API + 配置 + 预处理/后处理钩子实现需求。引擎问题尽量回馈上游。
**集成方式（v0.2 修订）**：M0 同时验证"Python API 嵌入"与"CLI 子进程调用"两条路径的可行性（进度获取、单页控制、错误恢复），M1 按验证结果定型；不把架构押在未验证的单一假设上。

### 3.3 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 翻译引擎 | BabelDOC（API 优先，CLI 兜底） | 见 3.2 |
| 后端 | Python 3.12（venv，**不用本机 3.14**，轮子兼容性风险）/ FastAPI / uvicorn | 异步 + SSE 方便；CPU 密集渲染丢线程池避免阻塞事件循环 |
| PDF 底层 | PyMuPDF（BabelDOC 内含） | 文本块坐标/字体提取能力最强 |
| 前端 | Jinja2 模板 + HTMX/原生 JS 起步，不满足再换 Vite+Vue | 自用工具避免前端工程化包袱 |
| 数据库 | SQLite | 单进程自用足够，零运维 |
| 任务队列 | 内存串行队列（asyncio.Queue）+ 状态落库 | 自用 v1 明确为串行执行；断点续翻按"批"记录状态 |
| 鉴权 | 共享 token（环境变量，FastAPI 中间件） | 公网部署最低限度防护，无账号体系 |
| 部署 | 本地：venv 直跑；服务器：Docker Compose | 本机暂无 Docker，服务器侧容器化 |

### 3.4 电子行业适配层设计（差异化核心）

**A. 不翻译规则引擎（对应红线 4，v0.2 扩展列级规则）**
- 片段级内置规则（正则 + 词表）：
  - 带单位数值：`±?\d+(\.\d+)?\s*(V|mV|A|mA|Ω|kΩ|MΩ|Hz|MHz|pF|nF|µF|ns|µs|ms|°C|dB|ppm|bit|GB|MB…)`（含工程变体 `3V3`）
  - 型号/封装：`SOP-8`、`QFN-32`、`STM32F103C8T6`、`ESP32-WROOM-32` 等
  - 总线/接口：`I²C`、`SPI`、`UART`、`JTAG`、`SWD`、`GPIO\d*`
  - 寄存器/位域命名（全大写含下划线，如 `USART_CR1_UE`）
  - 等宽字体文本块默认不翻译（代码/寄存器片段惯例）
- **表格列级规则**（v0.2 新增，回应审查 P1-4）：按列头词匹配整列策略——`Pin Name`/`Symbol`/`Min/Typ/Max` 列不翻，`Description`/`Function` 列翻
- **误杀审计**（回应审查 P2-1）：跳过内容按文档汇总展示（总数 + 抽样列表），支持"本页强制全翻"开关

**B. 术语表（对应红线 5，v0.2 修订实现通道）**
- 预置电子行业词表 300~500 条，**以行业惯用译法为准**（去耦电容/上拉电阻/阻焊层，而非字面直译），M2 建设，可参考开源电子词典/社区词表起步
- 实现通道（回应审查 P1-3）：
  - M0 验证引擎 glossary 语义；**不依赖"译后文本替换"**（会改变文本长度、破坏排版）
  - prompt 强约束注入（每批附带相关术语子集，按当前批次文本检索相关词条，避免全量注入浪费 token）
  - 译后校验：发现违反术语表的译文 → 标记告警/该片段重试，不做破坏版式的自动替换
  - 词形匹配规则：大小写不敏感、词边界匹配、复数归一
- 优先级规则（回应审查 P2-7）：**用户自定义 > 项目词表 > 全局预置**
- Web 界面 CRUD + JSON 导入导出

**C. 批处理对齐协议（v0.2 新增，回应审查 P1-1，表格红线的关键护栏）**
- 短文本块（表格单元格等）合并批次送 API 以省成本、补上下文
- 对齐保障：每段携带编号 ID 的请求/响应格式 → 返回解析校验（条数 + ID 一致）→ 校验失败自动降级逐条翻译 → 仍失败则该块标记告警，绝不盲目回填

**D. 质量护栏（对应红线 2/3/6）**
- **覆盖度报告**（v0.2 新增，回应审查 P1-2/P0 红线 6）：任务完成输出每页"可翻文本量 / 实际翻译量 / 疑似乱码页（ToUnicode 异常）/ 无文字层页"，异常页在结果页醒目提示
- 译文长度校验：译文明显长于原文框时缩字号或告警
- 图片对账：译前译后图片对象数量/几何对账
- 漏译对账：原文块数 = 译文块数，偏差超阈值告警
- 双语版式体检（回应审查 P1-6）：M0 专项观察密集表格/页底的双语表现；不达标页支持页级降级为单语译文

**E. 翻译缓存（v0.2 修订，回应审查 P1-5）**
- 缓存 key = 内容哈希 × 上下文指纹（前段哈希 + 所在页 + 是否表格内），避免一词多义被错误固化
- 术语表更新后可一键失效相关缓存

---

## 4. 里程碑 —— 实施状态（2026-08-29 同步）

> 实际开发与计划的差异：M0-M3 主体已提前完成；引擎已内建的能力（翻译缓存、批处理、分块）不再重复自研；计划外新增了服务监控页、端点连通性测试、协作式取消、目录中文化管线、中文覆盖校验、QPS 端点级配置。运行架构见 docs/architecture.md。

### M0 · 底座验证与 go/no-go ✅ 完成
- [x] 安装 BabelDOC，Python API 路径定型（结论：**go**）
- [x] 引擎探测结论与坑全部记录于 docs/m0-findings.md（事件流/水印默认开/Windows spawn 限制等）
- [x] 真实手册验证（SDS13782-4 + deepseek-v4-flash）：五条红线 + 双语版式 + glossary 语义全部过检

### M1 · 可用 Web + 安全与规模护栏 ✅ 完成
- [x] FastAPI + 共享 token 鉴权 + 上传上限（200MB/500页）+ SSE 进度 + 下载
- [x] 串行任务队列（断点续翻未做 → 见 v2 候选池，服务重启中断任务自动标记取消）
- [x] 试翻页范围、多端点管理 + 连通性测试、覆盖度报告、任务进度（块速/剩余时间/最后活动告警）
- [x] 服务监控页 /status、协作式取消、start.bat 日常启动

### M2 · 电子行业适配 ✅ 核心完成
- [x] 目录专项管线（计划外新增，用户核心诉求）：条目提取→编号协议批翻译→dual 右栏/mono 重绘，格式保留
- [x] 术语表：31 条预置 + Web CRUD + 引擎 glossary 注入 + 目录译文中文覆盖校验（豁免/严格重试/统计）
- [x] 批处理对齐协议：目录翻译已实现（编号 ID+校验+缺失重试）；**正文批处理为引擎内建能力，不再自研**
- [x] 翻译缓存：引擎自带（ignore_cache），不再自研；QPS 端点级可配（1-30）
- [ ] 不翻译规则引擎（正文片段级正则 + 表格列级）：暂以术语表 source=target 机制覆盖，正则版后延
- [ ] 误杀审计 / 本页强制全翻 / 页级单语降级：未做（引擎双语版式实测未触发红线）
- 验收结果：用户实测认可（"感觉还可以"）；目录 106/106 校验合格

### M3 · 打磨与部署 ✅ 主体完成
- [x] 双语/单语双输出、批量上传、失败一键重试、中文页面覆盖率
- [x] Dockerfile + docker-compose + docs/deploy.md（本机无 Docker，实机构建待服务器验证）
- [ ] 断点续翻、金标准回归脚本 → v2

### v2 候选池
断点续翻（分片+已完成分片缓存）｜扫描件 OCR｜图片内文字翻译｜术语自动学习｜多人/独立账号｜Claude/Gemini 原生适配｜正文不翻译正则引擎｜目录页右栏副本清空后处理

---

## 5. 风险与对策（v0.2 更新）

| 风险 | 概率 | 对策 |
|------|------|------|
| 引擎对复杂表格仍有错位 | 高 | M0 优先体检；列级规则 + 降级方案（表格页不翻译/页级单语）；go/no-go 兜底 |
| 批处理对齐失败导致内容串行 | 中 | 编号 ID 协议 + 返回校验 + 自动降级逐条（3.4-C） |
| 静默失败（无文字层/乱码页漏翻） | 中 | 覆盖度报告逐页提示，红线 6 拦截 |
| 术语漂移（分块上下文丢失） | 中 | prompt 注入相关术语子集 + 译后校验告警 + 表头强制一致 |
| 大文档（500 页+）内存/中断 | 中 | 按页分批 + 断点续翻进 v1 必备（需求 8） |
| 公网滥用（无账号体系） | 中 | 共享 token + 上传上限 + 串行队列天然限流 |
| Python 3.14 轮子兼容 | 低 | venv 固定 3.12 |
| 中文字体缺字/授权 | 低 | 引擎字体嵌入；确认默认字体开源授权（思源系列） |
| API 限流/计费失控 | 中 | 并发信号量 + 翻译缓存 + 任务级 token/费用统计（usage 缺失按字符估费） |
| 上游 BabelDOC API 变动 | 低 | 锁定版本；金标准回归；CLI 调用方式天然隔离部分变动 |

---

## 6. 关键决策记录

1. **二开而非自研**：BabelDOC 已解决最难的 70%；自研周期 3 个月起且 PDF 格式坑极多。
2. **双语对照而非纯译文**：使用场景是"查参数做设计"，译文存疑必须能回查原文。
3. **v1 不做 OCR/图内文字**：先把 80% 场景做到 95 分。
4. **术语双通道但不用译后替换**：纯 prompt 在长文档下不可靠；译后文本替换会破坏排版，改为"prompt 强约束 + 译后校验告警"（v0.2 修订）。
5. **公网 + 共享 token 而非零鉴权/账号体系**（v0.2 决策）：自用场景账号体系过重，但裸奔上公网不可接受；一个环境变量中间件是复杂度与安全的平衡点。
6. **500 页+ 为 v1 必备**（v0.2 决策）：协议手册场景明确，按页分批 + 断点续翻因此从"优化项"提为"必备项"。
7. **接受 L3 页面重排为已知限制**（v0.2 决策）：双语模式的固有代价，换取实现简单可靠；结果页展示受影响页清单保障知情。
8. **多端点多模型，任务级选择**（v0.2 决策）：性价比档位由使用者按文档重要性自定，配置页含单价信息支撑决策。

---

## 7. 行动清单（已执行完毕，留档）

1. ~~安装 BabelDOC、命令行试翻~~ → 完成（M0）
2. ~~收集代表性手册体检~~ → 完成（真实手册 SDS13782-4 全流程验证）
3. ~~产出差距清单、go/no-go 决策~~ → go，见 docs/m0-findings.md
4. ~~M1-M3 开发~~ → 主体完成，日常使用入口 `start.bat`；服务器部署见 docs/deploy.md
